<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head') %>
  <body>
    <%- include('../partials/header') %>
    <div class="container page">
      <div class="row">
        <div class="col-12 col-md-8 order-2 order-md-1">
          <div class="card page-card">
            <div class="card-body">
              Add User, <%= sender.username %> to your network?
              <form
                action="/requests/connectionRequests/<%= sender._id %>/decline"
                method="POST"
              >
                <button type="submit" class="form-button">Decline</button>
                <a href="/requests"
                  ><button type="button" class="form-button">Cancel</button></a
                >
              </form>
            </div>
          </div>
        </div>
        <%- include('../partials/sidebar') %>
      </div>
    </div>
  </body>
</html>
