<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <title>Dashboard</title>
  </head>
  <body>
    <div class="container"><%- include('../partials/header') %></div>
    <div class="container page">
      <div class="row">
        <div class="col-12 col-md-8 order-2 order-md-1">
          <div class="card page-card">
            <div class="card-body">
              <div class="card-title"><h3>Delete Project</h3></div>
              <p class="card-text">
                Are you sure you want to delete the project, <%= project.name
                %>? Once deleted all of the project data will be lost./p>
              </p>

              <form action="/projects/<%= project._id %>/delete" method="POST">
                <a href="/projects/<%= project._id %>">
                  <button class="form-button" type="button">Cancel</button></a
                >
                <button class="form-button" type="submit">Delete</button>
              </form>
            </div>
          </div>
        </div>
        <%- include('../partials/sidebar') %>
      </div>
    </div>
  </body>
</html>
